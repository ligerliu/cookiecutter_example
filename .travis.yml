language: python
python:
  - 3.6
cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/.ccache  # https://github.com/travis-ci/travis-ci/issues/5853

install:
  # Install this package and the packages listed in requirements.txt.
  - pip install .
  # Install extra requirements for running tests and building docs.
  - pip install -r requirements-dev.txt

env:
global:
  # Doctr deploy key for ligerliu/package_example/
  - secure: "<REDACTED>"
  
script:
  - coverage run -m pytest  # Run the tests and check for test coverage.
  - coverage report -m  # Generate test coverage report.
  - codecov  # Upload the report to codecov.
<<<<<<< HEAD
  - flake8 --max-line-length=315  # Enforce code style (but relax line length limit a bit).
  - make -C docs html  # Build the documentation.
  - pip install doctr
  - dotr deploy --built-docs docs/build/html .
=======
  #- flake8 --max-line-length=115  # Enforce code style (but relax line length limit a bit).
  - set -e
  - make -C docs html
  - pip install doctr
  - doctr deploy --built-docs /Users/jiliangliu/package_example/docs/build/html/index.html --deploy-repo ligerliu/package_example /Users/jiliangliu/package_example/
>>>>>>> gisaxs-converter-function
